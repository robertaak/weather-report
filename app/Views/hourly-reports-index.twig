<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width = device width, initial scale =1">
    <meta name="author" content="Roberta">
    <meta name="description" content="Hourly Weather Forecast">
    <title>Hourly Weather Forecast</title>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@1,700&family=Saira:ital,wght@0,100;1,400&display=swap" rel="stylesheet">
</head>

<body>
<header class="header-left">
    <h1 class="site-title">Hourly Report</h1>
</header>

{% for hourlyReport in hourlyReports %}

<div class="row">
    <div class="column">
        <div class="card">
            <img src="{{ hourlyReport.icon }}"  alt="a beautiful image">
            <p class="card-text">{{ hourlyReport.feelsLikeC }}</p>
            <p class="card-text">{{ hourlyReport.tempC }}</p>
            <p class="card-text">{{ hourlyReport.time }}</p>

        </div>
    </div>
    {% endfor %}
</div>

<footer>
    <h3 class="go-back"><a href="#">Back to top</a></h3>
</footer>
</body>
</html>