<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width = device width, initial scale =1">
    <meta name="author" content="Roberta">
    <meta name="description" content="Hourly Weather Forecast">
    <title>Weather Forecast in Riga</title>
    <link rel="stylesheet" href="/app/Views/Style/main.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;700&display=swap" rel="stylesheet">
</head>

<body>
<header class="header">
    <h1 class="site-title">WEATHER FORECAST</h1>
        <h2 class="city">RIGA, LATVIA</h2>
</header>

{% for hourlyReport in hourlyReports %}
    <div class="card">
        <div class="card-body">
            <img src="{{ hourlyReport.icon }}" alt="" class="icon">
            <p class="card-temp"><strong>{{ hourlyReport.tempC }}°C</strong></p>
            <p class="card-time">{{ hourlyReport.time }}</p>
        </div>
    </div>

{% endfor %}
</body>
</html>